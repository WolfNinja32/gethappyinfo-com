__pycache__/
*.pyc
.DS_Store

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# local-only brand sources (not served, not in build).
# test/ holds brand PNG mockups (ignored) AND the updater test suite (tracked).
assets/
test/*
!test/*.py
